version: "3"

services:
  webserver:
    build: ./nginx/
    ports:
      - "80:80"
      - "443:443"
    links:
      - flask
    depends_on:
      - flask
  flask:
    build: ./app/
    restart: on-failure
    ports:
      - "5000:5000"
    links:
      - database
    depends_on:
      - database
  database:
    image: mariadb
    ports:
      - "3306:3306"
    restart: on-failure
    environment:
      - MYSQL_ROOT_PASSWORD=root-password
      - MYSQL_DATABASE=firewagon